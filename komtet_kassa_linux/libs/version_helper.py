def greater_or_equal(current_version, version):
    return current_version >= version


def greater(current_version, version):
    return current_version > version


def less_or_equal(current_version, version):
    return current_version <= version


def less(current_version, version):
    return current_version < version
